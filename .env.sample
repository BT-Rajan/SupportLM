# --- Database ---
DB_HOST=localhost
DB_PORT=3306
DB_NAME=knowledgelm
DB_USER=root
DB_PASSWORD=

# --- Chat LLM provider (DeepSeek) ---
LLM_API_KEY=yourDeepSeekAPIKeyHere
LLM_CHAT_MODEL=deepseek-chat

# --- Chat LLM: OpenAI/Anthropic global fallback keys (Phase 4 — 2.0) ---
# Only needed if a tenant is configured (via the admin console or
# POST /api/tenant/llm-config) to use one of these providers WITHOUT
# supplying their own API key. Leave blank if every tenant either uses
# DeepSeek or brings their own key for OpenAI/Anthropic.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=YourAnthropicAPIKeyHere
# --- Embeddings ---
# Runs LOCALLY via sentence-transformers — no API key needed.
# DeepSeek does not offer a public embeddings endpoint, so this is
# intentionally separate from the chat provider above.
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2

# --- App ---
APP_SECRET_KEY=changeme-generate-a-real-random-value
APP_ENV=development

# --- SMTP (Gmail) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sreeni0076@gmail.com
SMTP_PASSWORD=krbgleeumkmqdkog
SMTP_FROM=sreeni0076@gmail.com
SMTP_USE_TLS=true
